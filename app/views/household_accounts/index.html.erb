<%= render 'shared/header' %>

<div class="main-content">
  <!-- メインビジュアル -->

  <!-- 家計簿投稿 -->
  <div class="post-section">
    <div class="post-content">
      <div class="post-text">
        <h2 class="main-title">家計簿を投稿しよう</h2>
        <p class="sub-title">家計簿を共有して、家計管理を改善しましょう。</p>
      </div>
      <div class="post-button-container">
        <%= link_to "家計簿を投稿する", new_household_account_path, class: "post-button" %>
      </div>
    </div>
  </div>

    <!-- ３つの特徴セクション -->
  <div class="section features">
    <h2>３つの特徴</h2>
    <div class="feature-list">
      <div class="feature-item">
        <h3>予算と家計簿の登録が可能</h3>
        <p>簡単に予算と家計簿を登録することができます。</p>
      </div>
      <div class="feature-item">
        <h3>一覧表示でわかりやすく振り返れる</h3>
        <p>家計簿を一覧表示することで、過去の支出を簡単に振り返ることができます。</p>
      </div>
      <div class="feature-item">
        <h3>予算を達成するとランクが上がる</h3>
        <p>設定した予算を達成することで、ユーザーのランクが上がります。</p>
      </div>
    </div>
  </div>
  
  <!-- 家計簿一覧 -->
  <div class="section household-accounts">
    <h2>家計簿一覧</h2>
    <ul class="household-accounts-list">
      <% @household_accounts.each do |account| %>
        <% result = @results[account.id] %>
        <li class="household-account">
          <span class="account-year"><%= account.created_at.year %>年 <%= account.month.name %></span>
          <span class="account-total">合計：<%= number_with_delimiter(account.total) %>円
            <span class="<%= result == '達成' ? 'achieved' : 'not-achieved' %>">
            (<%= result %>)
            </span>
          </span>
          <span class="account-description"><%= truncate(account.description, length: 30) %></span>
          <span class="account-link"><%= link_to '詳細を見る', household_account_path(account), class: 'btn btn-secondary' %></span>
        </li>
      <% end %>
    </ul>
  </div>
  <!-- 家計改善のコツ -->
  <div class="section tips-container">
    <h2>家計改善のコツ</h2>
    <div class="tips-list">
      <div class="tip">
        <h3>節約のコツ</h3>
        <p>毎日の支出を見直し、小さな節約を積み重ねることが大切です...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
      <div class="tip">
        <h3>貯金のコツ</h3>
        <p>毎月一定額を自動的に貯金する仕組みを作りましょう...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
      <div class="tip">
        <h3>投資の始め方</h3>
        <p>初めての投資は少額から始め、リスクを分散させることが重要です...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
      <div class="tip">
        <h3>光熱費の節約術</h3>
        <p>光熱費の無駄を見直し、効率的に節約する方法を紹介します...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
      <div class="tip">
        <h3>食費の削減方法</h3>
        <p>食材の買い方や調理方法を工夫して、食費を抑えるアイデアを共有します...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
      <div class="tip">
        <h3>通信費の節約</h3>
        <p>スマホ代やインターネット代を見直し、通信費を節約する方法を紹介します...</p>
        <%= link_to '続きを読む', '#', class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>

<%= render 'shared/footer' %>